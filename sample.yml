irc:
  networks:
    cognet:
      nick: 'chansey2'
      user: 'chansey2'
      fullname: 'Chansey v2'
      servers: ['irc.tamu.edu']
      channels: ['#bottest']

plugins:
  plugins: ['/tmp/test.rb']
